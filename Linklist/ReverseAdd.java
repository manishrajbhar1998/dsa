import java.util.*;

class Node {

  int data = 0;
  Node next;

  Node(int data) {
    this.data = data;
  }
}

class ReverseAdd {

  public static void main(String[] args) {
    // Node n1 = new Node(2);
    // Node n2 = new Node(4);
    // Node n3 = new Node(3);

    // n1.next = n2;
    // n2.next = n3;

    // Node temp = n1;
    // while (temp != null) {
    //   System.out.print(temp.data + " ");
    //   temp = temp.next;
    // }

    // Node head = n1;
    // Node prev, curr, next;
    // prev = null;
    // curr = head;
    // next = head.next;

    // while (curr != null) {
    //   curr.next = prev;
    //   prev = curr;
    //   curr = next;
    //   if (next != null) {
    //     next = next.next;
    //   }
    // }

    // String a = "";
    // System.out.println();
    // while (prev != null) {
    //   System.out.print(prev.data + " ");
    //   a = a + prev.data;
    //   prev = prev.next;
    // }

    // System.out.println(a);
    // int val = 10 + Integer.parseInt(a);
    // System.out.println(val);

    
  }
}
